services:
  saleor-meili-app:
    container_name: saleor-meili-app
    build:
      context: .
      dockerfile: Dockerfile
    restart: always
    ports:
      - "7000:7000"
    env_file:
      - ./stack.env
    depends_on:
      meilisearch:
        condition: service_healthy
    networks:
      - traefik-net
    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=traefik-net"
      - "traefik.http.routers.saleor-meili.rule=Host(`ecommerce-search.aahrbitx.in`)"
      - "traefik.http.routers.saleor-meili.entrypoints=websecure"
      - "traefik.http.routers.saleor-meili.tls=true"
      - "traefik.http.routers.saleor-meili.tls.certresolver=letsencrypt"
      - "traefik.http.services.saleor-meili.loadbalancer.server.port=7000"

  meilisearch:
    image: getmeili/meilisearch:v1.6
    restart: always
    env_file:
      - ./stack.env
    environment:
      - MEILI_ENV=production
      - MEILI_NO_ANALYTICS=true
    volumes:
      - meili_data:/meili_data
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:7700/health"]
      interval: 5s
      timeout: 2s
      retries: 5
    networks:
      - traefik-net

volumes:
  meili_data:

networks:
  traefik-net:
    external: true
